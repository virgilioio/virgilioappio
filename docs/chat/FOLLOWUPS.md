# Chat Module — Follow-ups Log

Out-of-scope observations made during Chat implementation. Anything noticed but **not** in the current step's `Files allowed` manifest is logged here instead of fixed in-flight. Items are triaged after each phase ships.

Format per entry:

```
- [YYYY-MM-DD] [Step X.Y] <observation>
  - File(s): <path>
  - Suggested action: <follow-up>
```

---

(no entries yet)
