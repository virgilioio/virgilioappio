

# Change Dotted Map Base Dots to Lilac

Single-line change in `src/components/talent-insights/GeographyInsights.tsx`.

In the `map.getSVG()` call, change the `color` from `'hsl(var(--muted-foreground) / 0.25)'` to a lilac tone like `'hsl(267, 80%, 85%)'` (a soft pastel lilac consistent with the Virgilio purple palette).

