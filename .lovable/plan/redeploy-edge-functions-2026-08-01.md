# Redeploy edge functions

Redeploy the `create-booking` and `process-transcript-webhook` Supabase Edge Functions without any code changes, then confirm deployment success and report the deployed version number for each function.

No files will be modified; this is a pure deployment operation.